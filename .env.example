# Local Online Judge Environment Configuration
# Copy this file to .env and modify the values as needed

# Database Configuration
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_NAME=cp_platform

# Security Configuration
SESSION_SECRET=your_very_secure_session_secret_here_minimum_32_characters

# Application Configuration
PORT=3000
MAX_SOURCE_LENGTH=50000

# File Upload and Processing
BULK_ROOT=/path/to/allowed/bulk/import/directory

# Optional: Performance tuning
NODE_ENV=production

# Database connection limits (optional)
# DB_CONNECTION_LIMIT=20
# DB_ACQUIRE_TIMEOUT=60000

# Security Notes:
# 1. Change SESSION_SECRET to a long, random string
# 2. Use a strong database password
# 3. Consider running the application in a containerized environment
# 4. Never commit this file to version control if it contains real credentials